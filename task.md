# 任务记录

## 已完成
- [x] 将项目中所有的 `MiaoBuy`/`MiaoGo` 替换为 `FlashGo`。
- [x] 增加系统通知功能，反馈自动运行状态（执行识别、执行成功）。
- [x] 修复 `window.ai` 在内容脚本中受限问题：重构 AI 架构，通过 `chrome.scripting.executeScript` 在 Main World 注入执行。
- [x] 为“自动完善”按钮及“单次任务”补充多语言 (i18n) 适配。
- [x] 在 `manifest.json` 中补充 `scripting` 权限以支持 Main World 注入。
